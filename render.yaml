services:
  - type: web
    name: quantum-bridge
    runtime: node
    plan: free
    buildCommand: npm ci && npm run build
    startCommand: npm run api
    healthCheckPath: /api/health
    envVars:
      - key: NODE_VERSION
        value: "22"
      - key: DATABASE_URL
        fromDatabase:
          name: quantum-bridge-postgres
          property: connectionString
      - key: PGSSLMODE
        value: disable
      - key: QUANTUM_WORKER_DISABLED
        value: "1"
      - key: IRIS_API
        value: https://iris-api-sandbox.circle.com
      - key: CORS_ORIGIN
        value: "*"

  - type: worker
    name: quantum-bridge-worker
    runtime: node
    plan: free
    buildCommand: npm ci
    startCommand: npm run worker
    envVars:
      - key: NODE_VERSION
        value: "22"
      - key: DATABASE_URL
        fromDatabase:
          name: quantum-bridge-postgres
          property: connectionString
      - key: PGSSLMODE
        value: disable
      - key: IRIS_API
        value: https://iris-api-sandbox.circle.com

databases:
  - name: quantum-bridge-postgres
    databaseName: quantumbridge
    user: quantumbridge
    plan: free
